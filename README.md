# git-scripts-workbench

This repository contains handy `git` scripts in various states of completion.

## mmerge.sh

This script automatically performs the steps to merge the master branch into your current branch.
